{
  "name": "matchblade",
  "version": "0.0.1",
  "description": "A match-3 dungeon runner — slide tiles to fight your way through the dungeon.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  }
}
